# human/data

Small version-controlled inputs (bmmc_gene_list.tsv, BMMC_gene_list.txt).
Large external datasets (Granja h5ad, GTEx v11, Lambert TF list, etc.) are
NOT committed; download them here per the table in ../README.md. They are
git-ignored.
